function rectanglesArea() {
    var width = jsConsole.readFloat("#width");
        height = jsConsole.readFloat("#height");

        jsConsole.writeLine("The rectangle's area is equal to " + width*height);
}